
function H() {

  return ( 
    <>
      {/* <h1 className="text-zinic-900" contentEditable="true">What is JSX?
JSX stands for JavaScript XML.

JSX allows us to write HTML in React.

JSX makes it easier to write and add HTML in React.

</h1>
<h2>Expressions in JSX
With JSX you can write expressions inside curly braces { }.

The expression can b</h2>
<h1>Attribute class = className
The class attribute is a much used attribute in HTML, but since JSX is rendered as JavaScript, and the class keyword is a reserved word in JavaScript, you are not allowed to use it in JSX.

Use attribute className instead.

JSX solved this by using className instead. When JSX is rendered, it translates className attributes into class attributes.</h1>
   <h4 contentEditable="true">
   1- contentEditable use to edit line 
   </h4>
    <h4>
      image close self 
     
      </h4>
  

<h1>
  inline style 
  <h1
  style={
 {   backgroundColor:'pink',
 textAlign:'center',
 fontSize:'20px'}
  }>Hy today cover inline css </h1>
</h1> */
<h1>asdh</h1>}
</>
  );
}

export default H;